<!--index.wxml-->
<view class="container">
	<text class="tips">
    功能模块演示
  </text>

	<view
	 wx:key="{{ index }}"
	 wx:for="{{ modules }}"
	 data-url="{{ item[1] }}"
	 bindtap="jumpToChildModule"
	>
		{{ item[0] }}
	</view>
</view>

